#include "main.h"

/**
 * _memcpy - returns the largest of 3 numbers
 * @dest: first intege
 * @src:d
 * @n:d
 * Return: largest number
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		dest[i] = src[i];

	return (s);
}
