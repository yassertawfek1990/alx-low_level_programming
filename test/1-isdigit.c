#include "main.h"


/**
 * _isdigit - returns the largest of 3 numbers
 * @c: character
 * Return: largest number
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}



