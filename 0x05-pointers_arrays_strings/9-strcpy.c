#include "main.h"

/**
 * _strcpy - returns the largest of 3 numbers
 * @dest: first integer
 * @src: second intege
 * Return: largest number
 */

char *_strcpy(char *dest, char *src)
{
	dest = src;

	return (dest);
}
