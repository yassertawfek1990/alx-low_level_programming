#include "main.h"

/**
 * print_rev - returns the largest of 3 numbers
 * @s: first integ
 */

void print_rev(char *s);
{
	int i;
	int l = strlen(str);

	for (i = l-1; i >= 0; i--)
		_putchar(str[i]);
	_putchar('\n');
}

